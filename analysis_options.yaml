analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
  errors:
    avoid_print: warning

linter:
  rules:
    - avoid_print
    - prefer_const_constructors
    - use_build_context_synchronously
